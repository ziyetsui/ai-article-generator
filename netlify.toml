# Netlify Deployment Configuration

# Base build settings for the production environment
[build]
  # Command to build the frontend application
  command = "cd frontend && npm install && npm run build"
  
  # Directory where the built frontend files are located
  publish = "frontend/build"
  
  # Directory where the serverless functions are located
  functions = "api"

# Redirects and rewrites rules are processed in the order they appear.

# Rule for submitting a new job
[[redirects]]
  from = "/api/submit-job"
  to = "/.netlify/functions/submit-job"
  status = 200 # POST

# Rule for checking job status
[[redirects]]
  from = "/api/check-status"
  to = "/.netlify/functions/check-status"
  status = 200 # GET

# The background function does not need a public URL, it's invoked internally.

# Rule for Single Page Application (SPA) routing
# This should be the last rule.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
